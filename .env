# MCP Environment Configuration
# Updated: June 2025
# 
# This file contains all environment variables used by MCP.
# Variables marked [REQUIRED] must be set for the system to function properly.
# Variables marked [OPTIONAL] have sensible defaults if not specified.

# =====================================================================
# PROJECT CONFIGURATION
# =====================================================================
COMPOSE_PROJECT_NAME=mcp                     # [REQUIRED] Docker Compose project name
COMPOSE_FILE=docker-compose.yml              # [REQUIRED] Docker Compose file path

# =====================================================================
# SERVER CONFIGURATION
# =====================================================================
MCP_HOST=0.0.0.0                             # [REQUIRED] Host to bind MCP server
MCP_PORT=8811                                # [REQUIRED] Port for MCP server
MCP_ENVIRONMENT=production                   # [OPTIONAL] Environment (production/development/testing)

# =====================================================================
# NETWORK CONFIGURATION
# =====================================================================
MCP_NETWORK=mcp-network                      # [REQUIRED] Docker network name
MCP_SUBNET=172.40.1.0/24                     # [REQUIRED] Docker subnet CIDR

# =====================================================================
# APPLICATION CONFIGURATION
# =====================================================================
LOG_LEVEL=info                               # [OPTIONAL] General logging level
MCP_DATA_DIR=./data                          # [REQUIRED] Data directory path
MCP_CACHE_DIR=./cache                        # [REQUIRED] Cache directory path

# =====================================================================
# LOGGING
# =====================================================================
MCP_LOG_LEVEL=info                           # [OPTIONAL] MCP-specific logging level
MCP_LOG_FORMAT=json                          # [OPTIONAL] Log format (json/text)
MCP_VERBOSE_LOGGING=false                    # [OPTIONAL] Enable verbose logging

# =====================================================================
# SECURITY
# =====================================================================
MCP_ENABLE_CORS=true                         # [OPTIONAL] Enable CORS support
MCP_ENABLE_RATE_LIMITING=true                # [OPTIONAL] Enable rate limiting

# =====================================================================
# MONITORING
# =====================================================================
MCP_ENABLE_METRICS=true                      # [OPTIONAL] Enable metrics collection
MCP_ENABLE_HEALTH_CHECK=true                 # [OPTIONAL] Enable health check endpoint

# =====================================================================
# RESOURCE LIMITS
# =====================================================================
MCP_MEMORY_LIMIT=2G                          # [OPTIONAL] Container memory limit
MCP_CPU_LIMIT=2.0                            # [OPTIONAL] Container CPU limit

# =====================================================================
# FILE PATHS (Platform-specific)
# =====================================================================
MCP_CONFIG_PATH=c:\Users\ofayese\.docker\mcp\config.yaml   # [REQUIRED] Config file path
MCP_SECRETS_PATH=c:\Users\ofayese\.docker\mcp\secrets      # [REQUIRED] Secrets directory path
MCP_REGISTRY_PATH=c:\Users\ofayese\.docker\mcp\registry.yaml # [REQUIRED] Registry file path

# =====================================================================
# DATABASE CONFIGURATION
# =====================================================================
POSTGRES_DB=mcp                              # [REQUIRED] PostgreSQL database name
POSTGRES_USER=mcp                            # [REQUIRED] PostgreSQL username
POSTGRES_PASSWORD=mcp_password               # [REQUIRED] PostgreSQL password
POSTGRES_HOST_AUTH_METHOD=scram-sha-256      # [OPTIONAL] Authentication method

# =====================================================================
# REDIS CONFIGURATION
# =====================================================================
REDIS_PASSWORD=mcp                           # [REQUIRED] Redis password

# =====================================================================
# DOCKER CONFIGURATION
# =====================================================================
DOCKER_BUILDKIT=1                            # [OPTIONAL] Enable BuildKit
COMPOSE_DOCKER_CLI_BUILD=1                   # [OPTIONAL] Use Docker CLI for builds
DOCKER_SOCKET_PATH=/var/run/docker.sock      # [OPTIONAL] Docker socket path

# =====================================================================
# EXTERNAL SERVICES
# =====================================================================
EXTERNAL_NETWORK=docker_default              # [OPTIONAL] External Docker network name

# =====================================================================
# FEATURE FLAGS
# =====================================================================
MCP_EXPERIMENTAL_FEATURES=false              # [OPTIONAL] Enable experimental features
MCP_DEBUG_MODE=false                         # [OPTIONAL] Enable debug mode
